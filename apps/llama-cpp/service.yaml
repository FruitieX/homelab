apiVersion: v1
kind: Service
metadata:
  name: llama-cpp
  namespace: default
spec:
  selector:
    app: llama-cpp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http